/**
 * @elevator-sim/experiments — public barrel.
 *
 * Replication runner, CRN management, statistics (paired-t, sequential stopping,
 * saturation detection) and run-record persistence. Every persisted run record carries
 * the seed that produced it.
 *
 * May depend on `@elevator-sim/core`. Re-export runner/, stats/ and reports/ here as
 * those modules land.
 */

export {};

/**
 * @elevator-sim/cli — headless batch entry point.
 *
 * Argument parsing, config loading and run/sweep invocation live here. Wall-clock time
 * and process I/O are allowed in this package; they are not allowed in
 * `@elevator-sim/core`.
 */

export {};

# Risk register

> ## ↩️ Reopened 2026-07-30 for **wave 11**. Three risks were realised that this register had no row
> ## for, and all three are properties of *how the work is done* rather than of the simulator.
>
> | # | Risk | State |
> |---|---|---|
> | **R24** | **A search tool that fails silently makes every negative finding unreliable, including the ones already written down.** `grep` here wraps `ugrep -I`, which skips files it deems binary by printing *nothing* — exit 1, indistinguishable from a genuine miss. Five source files carried raw NUL bytes, among them two pin guards and the § D163 clause-2 parity check. | **Realised twice.** A false *"nothing writes `#rail-access-note`"*, caught within the hour by driving the page; and a false [`GAPS.md`](GAPS.md) entry claiming the matrix pins were re-derived by no test — which **sat in the register** where a lane would plan against it. Closed at the cause in `f78dc42`. **Mitigation:** a negative result from a search tool is evidence about the tool as well as the tree; confirm *"nothing does X"* by driving, by types, or by a reader that fails loudly. **Escalation:** any register entry whose only evidence is a search that found nothing. |
> | **R25** | **File-level lane ownership partitions *editing* and does not partition *committing*.** `git add -A` stages the whole repository regardless of who owns what. | **Realised.** `ae6750b` describes one lane and contains three ([§ D182](DECISIONS.md)). Nothing was lost and nothing was unreviewed, but the commit is not evidence of what landed together. **Mitigation:** one worktree per concurrent lane — the wave's own policy, not followed — or, failing that, explicit paths at every `git add`. **Escalation:** any commit whose diff touches a file its message does not mention. |
> | **R26** | **A test suite built entirely from fixtures cannot distinguish *the mechanism is correct* from *the mechanism is reached*.** | **Realised.** Every assertion in `mode/disclosure.test.ts` handed a suppression ground in directly, and all of them were green for a whole commit while the shipped screen rendered the ground-free lead ([§ D185](DECISIONS.md)). This is the standing requirement's *reachable* vs *has a non-test caller* distinction arriving through a fixture instead of a barrel re-export. **Mitigation:** at least one case per surface must originate in a real run. **Escalation:** a suite that would stay green with the production wiring removed. |
>
> **R1, R5, R7 and R10 below remain live and permanent.**

> ## ↩️ The delivery reopened 2026-07-28 as **wave 5**, and this register stays retired in place.
>
> Wave 5's own risks are in [`WAVE5_PLAN.md`](WAVE5_PLAN.md) § 7 — four of them concrete and
> task-specific (a shifted fuzz seed→case mapping silently re-pointing two named regressions; a
> tautological phase-status guard; a schema relaxed so a UX row can be ticked; a UX row argued rather
> than exercised), plus the register-shrinks-to-look-finished risk that § 5 answers by requiring the
> debt table to be rewritten to what is *actually* left.
>
> **R1, R5, R7 and R10 below are live and permanent** — they are properties of the project, not of
> the closed delivery — and they still apply to wave 5.

> ## 🏁 FINAL STATE of waves 1–4 — that delivery closed 2026-07-28. This section is no longer updated.
>
> **What it was for.** Naming, in advance, the ways this delivery could produce confident nonsense
> or quiet damage, with a mitigation and an escalation trigger for each — so that a risk becoming
> real was a *recognised event* rather than a surprise. Eight of the twenty-three were **realised**,
> which is the register working, not failing.
>
> **Disposition at close.**
>
> | | IDs |
> |---|---|
> | ✅ **Closed or discharged** | R2, R3, R4, R6, R8, R9, R11, R12, R13, R14, R15, R16, R17, R18, R19, R20, R21, **R22**, **R23** |
> | 🟡 **Live and permanent** — these are properties of the project, not of the delivery | R1, R5, R7, R10 |
>
> **R22 is the one worth reading.** It said: *an open Phase 8 property violation is closed by
> weakening the property rather than by fixing the simulator*, and named the exact cheap fix —
> `deadlockIdleBoundS` is a single number and moving it makes the red go away. **Discharged by
> measurement:** the bound is untouched at 600 s, `PROPERTY_BOUNDS` is unchanged line for line, the
> case was not filtered out of the corpus, and the property gained no exemption. `fuzz-1000384` was
> fixed in `sim/` (§ T22-D1).
>
> **R23 is the other.** It said a refuted mechanism would be re-asserted because nothing pinned the
> correction. **Closed:** `validation/documentation.test.ts` now pins all seven sites three ways,
> and all three were watched failing.
>
> **The four that stay live are the four that were never about this delivery.** R1 (a ninth dead
> seam), R5 (statistical nonsense), R7 (an agent reports a green suite that is red) and R10 (scope
> pressure weakens a criterion) describe how *this kind of work* fails. R10 was realised once, by
> the orchestrator, inside a decision meant to strengthen a gate — see `DECISIONS.md` § D99.
>
> > ## ⚠️ **R1 was realised — twice — after this register closed.** It stays **live**, because it is
> > a property of the project rather than of the delivery, and because being realised is what a
> > high-likelihood risk does.
> >
> > 1. **The ninth in code was a whole half of a directory, not a symbol.** Every study
> >    `published.ts` maps to a `PublishedStudyId` had `regeneratePins.ts` as its non-test caller;
> >    every study it maps to `'no-intervals'` had **none**, so all five were dead.
> >    `benchmark/livenessSuite.ts` is now that half's driver and the guard derives its entry-point
> >    set from the directory. [§ D114](DECISIONS.md).
> > 2. **And once in `data/`, which this register's wording did not anticipate.**
> >    `destination-eta` shipped `dispatch.callType: mobile-credential` with a weight vector
> >    identical to `eta`'s — schema-valid, separately tested, and **bit-identical to `eta` at 8 of 8
> >    matrix cells**. R1's mitigation column says *"every task names the file its behaviour is
> >    called from"*; a profile in a JSON file has no such file, and that is the gap. Closed by
> >    authoring `weights.rideTime: 0.5`. [§ D112](DECISIONS.md).
> >
> > **The mitigation that would have caught the second, stated for the next register:** a shipped
> > *configuration* needs the same liveness evidence a shipped *symbol* does — an evaluation count
> > with cross-car spread through a real run, and a flat gated-off side. `docs/09` § 8 R6-1 named
> > this exact shape as a risk and its mitigation *"was never wired to a test"*.
> >
> > **R7 was also realised, in a smaller form:** a brief conflated two study arms, and a tree was
> > reported clean when a file-sync client had polluted it. Both were caught by re-running rather
> > than re-reading. See `MULTI_AGENT_PLAN.md`'s mistakes 4 and 5.

**Status as of 2026-07-28, at close.** Closed: R2–R4, R6, R8, R9, R11–R23. Still live and permanent:
R1, R5, R7, R10.

| ID | Risk | Likelihood | Impact | Owner | Mitigation | Escalation trigger |
|---|---|---|---|---|---|---|
| R1 | **A ninth "configurable, tested in isolation, dead in the shipped path" defect ships.** Eight have already, and a *weaker* ninth is recorded rather than hidden: `fuzz/`'s only non-test caller is a test (**C24**) — defensible, because a fuzzer's product *is* a test, but it is not the answer `tune` gives `tuning/`. **⚠️ REALISED TWICE — see the header. Nine in code, one in `data/`.** | High — it is the project's dominant failure mode | Critical. Invalidates every number the feature is supposed to produce, and passes every check the repo runs | orchestrator | Every task names the file its behaviour is *called from*; T1 extends the mechanical dead-code audit to `experiments/src/tuning/**`; T3 adds a generic search-space liveness sweep so a flat dimension fails a test rather than waiting for a reviewer | Any task reports "wired" without an instrumented run or a trajectory-difference assertion |
| R2 | **T2's quantile fix silently invalidates published results.** Switching `pairedDifferenceEstimate` from z to t past n=25 widens every interval at the documented 50–200 budget | Certain (it is the point of the fix) | High. Verdicts flip; at least 3 of 148 real comparisons flipped in the review's own sweep | T2 + reviewer | T2 must enumerate every number published in `docs/` and `benchmark/` that its change moves, and hand the list to T4; T4 re-derives rather than copies | A published verdict flips from BETTER to INDISTINGUISHABLE and is not re-derived in docs |
| R3 | **T3's fixes to inert tunables change simulator behaviour**, invalidating benchmark numbers T4 is documenting | High | Medium. Rework, not wrong results — provided it is caught | T3 + orchestrator | Merge order puts T3 before T2 before T4; T3 reports every shipped-run trajectory change it causes | T3 reports a behaviour change on a shipped profile without a regenerated benchmark number |
| R4 | **Removing a dead dimension is mistaken for a fix.** Deleting `answer.reopenOnLateArrival` closes the finding without deciding whether late-arrival reopens *should* be modelled | Medium | Medium. Silently narrows the model and is unrecoverable from the register | T3 | Each inert dimension gets an explicit implement-vs-delete-vs-gate decision recorded in `DECISIONS.md`, not a default | T3 deletes a tunable with no recorded rationale |
| R5 | **Statistical nonsense is reported** — the failure mode `CLAUDE.md` names as most likely | Medium | Critical | every task | No comparison without a paired-t interval excluding zero; saturated configurations suppressed; CRN across arms; resolution limits from `docs/07-handoff.md` § 4 respected (0.20 s near-neighbour / 1.9 s structural at n=100) | Any report claims a win below the measured resolution limit |
| R6 | **Merge conflicts on shared files** across five concurrent worktrees | Low | Medium | orchestrator | Disjoint ownership map in `MULTI_AGENT_PLAN.md` § 5; fixed merge order; `--no-ff` merges so any one is revertable | Two tasks report writing the same file |
| R7 | **An agent reports a green suite that is red.** Has happened in this repository | Medium | High. A gate that does not gate | orchestrator | Orchestrator re-runs `npx tsc -b` and the full suite itself after every merge; reviewers are instructed to run, not read | A task's claimed result cannot be reproduced by the orchestrator |
| R8 | **Phase 4 UI is built without a UX cycle** and ships confusing flows | Medium | Medium | T5 → wave 2 | T5 locks the contract and the role/task/flow inventory before any UI fan-out; `TEST_MATRIX.md` carries scenario coverage including empty, loading, error and recovery states | Wave-2 UI work starts before the flow inventory exists |
| R9 | **Phase 6 changes the passenger model fundamentally** (destination known at call time) and breaks Phases 0–5 assumptions | Medium | High | wave 3 | Contract/architecture task before any implementation; compare on TTD not AWT, per `docs/07-handoff.md` § 7 | Phase 6 implementation starts without a locked passenger-model contract |
| R10 | **Scope pressure produces a weakened acceptance criterion** | Medium | Critical — it is how this project fails quietly | orchestrator | `CLAUDE.md`: raise a criterion, never lower it. A phase that cannot meet its gate is recorded as not meeting it — Phase 7 was NOT ACCEPTED until real work closed its blockers, and at close **Phase 8 was recorded partial rather than accepted** because one of its two acceptance clauses did not pass (§ D102) — and it was then closed by *doing the work*, not by deleting the clause: the eighth track landed in `f895a16` and the phase is accepted (§ D108). That is the outcome this risk control is for. **This risk was realised once**, by the orchestrator, in § D27 → § D99: a *building* clause dropped from a criterion inside a decision whose stated purpose was to strengthen it. Closed by measuring the dropped clause (§ D100), not by arguing it away | Any proposal to relax a stated criterion |
| R11 | **`package-lock.json` is stale against `packages/viz`'s `vite` devDependency**, so `npm ci` fails on a clean clone even though `npm install`-based development works | Certain until fixed | Medium — breaks CI and any fresh checkout, invisible to every local run | orchestrator | Deferred deliberately (D6) because the shared `node_modules` is symlinked into live worktrees; tracked as C1 and gating wave-1 closure | Wave 1 closes without C1 done |
| R12 | **Instrumentation perturbs the run it visualizes.** `viz/src/record/instrument.ts` monkey-patches five `Car` methods; if it changes behaviour, every frame shown is of a different simulation than the one the CLI measures | Low — a neutrality test exists | High — the viewer would silently disagree with the measurements | T5 → reviewer | Byte-identity of `RunRecord` and `PassengerTrace` against an uninstrumented run is asserted; independent reviewer instructed to re-test across more buildings/profiles/seeds than the test uses | Any building/profile/seed where instrumented and uninstrumented records differ |
| R13 | **A published number is transcribed from a document that reconstructed it from rounded inputs.** `T2-BLAST-RADIUS.md` §4 claimed to re-measure and instead derived new bounds arithmetically from already-rounded published bounds, which propagates ~100% of the lower bound's rounding error. 4 of 4 spot-checked values were wrong in the last printed digit, one contradicting §3 of the same document | Realised — caught by review before T4 ran | High. T4's entire job is transcription; wrong digits would enter `docs/` and code comments as measured facts | T6 | §4a/§4b regenerated from full-precision re-runs; anything not re-measured must be labelled NOT RE-MEASURED so T4 cannot transcribe it in good faith | T4 dispatched before T6 lands |
| R14 | **The seventh dead-seam instance, created by a fix.** T2's correction left `halfWidthQuantile` — the function implementing `docs/03` Part 3's sequential-stopping crossover — with no non-test production caller except a label fallback, while its own docstring names callers that do not exist and `productionStoppingRule` silently changed quantile family | Realised | High. It is the exact defect class the roadmap's standing requirement exists to prevent | T6 | Must be resolved as one change — either inject a crossover estimator into `productionStoppingRule`, or delete the function and correct `docs/03` Part 3. No symbol may be left claiming a role nothing gives it | T6 closes without naming the non-test caller or reporting the deletion |
| R15 | **A safety argument is believed rather than checked.** "Widening is one-way, so no claim gets stronger" is false: `pareto.ts:505 dominanceOf` can promote a candidate from `mutually-non-dominated` to `dominates` when a WORSE becomes INDISTINGUISHABLE | Realised | Medium — no published front is affected today, but the argument was being relied on | T6 | Argument retracted and qualified; the Pareto path checked rather than assumed | Any future widening change reuses the unqualified argument |
| R16 | **A knob's published cost is an artefact of its own implementation bug.** T3's courtesy hold re-granted the *whole-cohort* transfer dwell for a single late passenger, so `reopenOnLateArrival`'s measured cost (+59.1% AWT worst case) is a fiction — with the spurious dwell removed the same cells go *negative*, i.e. the hold may be a net benefit | Realised — caught by review | Medium today (the knob ships off, so no published number moves), High later (any tuning of it optimises against nonsense) | T7 | Pass a revised `DoorStopReason` sized to the passengers actually boarding; re-measure from scratch; add a test bounding a granted hold's added dwell by the passengers who actually board | Any figure for this knob is quoted before T7 re-measures |
| R17 | **`activeWhen` is an unfalsifiable escape hatch.** The liveness sweep imposes a proof obligation on `DECLARED_INERT` entries but none on `activeWhen`-gated regions, so a gate that hides a *live* dimension passes every test. Measured: `idle.predictorHorizonS` is live at `predictorCycleS` 3600 on `secure-tower` and the gate declares it dead | Realised | High — it reintroduces exactly the defect class the sweep exists to eliminate, through the sweep's own blind spot | T7 | Extend the sweep so every `activeWhen`-gated dimension has its gated-**off** region asserted flat | A gate is added without a flatness assertion on the region it excludes |
| R18 | **Published benchmark numbers that the code does not produce, with nothing in the suite able to fail on it.** Three separate instances are now known: register finding #4 (an n=300 interval published in an n=500 table), `runTailStudy()`'s figures (which differ in the *mean*, not just the half-width, so not attributable to the quantile change), and two `benchmark/index.ts` bounds that never reproduced | Realised — 3 known instances | High. The project's stated purpose is producing defensible numbers; a published figure the code disowns is the failure mode `CLAUDE.md` opens by naming | T9 | Establish root cause for each rather than overwriting; build a guard that re-derives every published study interval and fails on drift, verified by reintroducing all three | A number is corrected without its root cause being established, or the guard cannot catch all three known instances |
| R19 ✅ **CLOSED** — T9 confirmed determinism by measurement: 263 figures across all five studies hash identically within and across processes. Invariants 2 and 4 hold; this was never a reproducibility failure. | **A study might not be deterministic from its seed.** If `runTailStudy()`'s non-reproduction turns out to be non-determinism rather than a configuration change, it breaks invariants 2 and 4 and invalidates far more than the four numbers in question | Low — the determinism regression suite is green | Critical if realised | T9 | T9 is instructed to stop and treat it as the headline if found, rather than correcting the numbers | Any study produces different output from the same seed |
| R20 | **The orchestrator introduces drift when merging work whose author did not own the file that must change.** T14 wrote `docs/09` and could not update README's table; the merge left them inconsistent | Realised once, caught immediately | Low — the wave-1 guards now fail on it | orchestrator | `validation/documentation.test.ts` fails on any `docs/*.md` not linked from README, and on phase-set disagreement across `CLAUDE.md`/`README.md`/`docs/07`. When merging a task that reports doc handbacks, action them **in the merge**, not after | A post-merge suite goes red on a documentation guard |
| R21 ✅ **CLOSED** — D91. The always-on tier now asserts only *simulation outputs* (legs carried, kernel events), which are functions of the seed and identical on every machine at every load; every wall-clock gate moved to `ELEVATOR_SIM_DEEP=1`, where it reads the grid the always-on sweep already ran. No threshold was loosened a third time. The stated cost: event count catches a regression that creates more *work*, not one that makes each unit of work more expensive. | **A timing-based gate goes red under normal concurrent load.** `validation/perfScaling.test.ts` asserts wall-clock fit quality (R² > 0.9); it failed at 0.887 on `integration` while another builder's suite ran, and passed 5/5 twice in isolation | Realised | Medium — a flaky gate trains everyone to ignore red, which is worse than no gate | T20 | **Not** a third threshold loosening (two were already loosened and disclosed). Keep the deterministic claim always-on — cost regressed on *legs processed* is R² 0.998 and immune to contention — and move wall-clock fit-quality gates to the opt-in deep tier while still printing the numbers | Any timing assertion can fail a build on a busy machine, or a threshold is loosened a third time |
| R22 ✅ **DISCHARGED** — `fuzz-1000384` was fixed in `sim/` by § T22-D1. `deadlockIdleBoundS` is untouched at 600 s, `PROPERTY_BOUNDS` is unchanged line for line, the case is still in the corpus, and the property gained no exemption. Whatever fired before still fires. | **An open Phase 8 property violation is closed by weakening the property rather than by fixing the simulator.** `fuzz-1000384` is a P5 termination violation: 1 694.3 s of fleet inactivity with a servable journey outstanding. The bound it trips, `deadlockIdleBoundS`, is a single number and moving it makes the red go away | Medium — it is the cheap fix, and the finding is pre-existing so nobody's change caused it | **Critical.** Phase 8's entire value is that it fires on things nobody thought of; a bound raised to silence a real finding converts the campaign into decoration | T22 | The precedent is set and must be followed: D86 fixed a P5 blindness by making the property **strictly stronger** and left `deadlockIdleBoundS` at 600 s, and D91 refused a third threshold loosening. Any fix must show that whatever fired before still fires | The bound moves, the case is filtered out of the corpus, or the property gains an exemption whose only instance is this case |
| R23 ✅ **CLOSED** — `validation/documentation.test.ts` pins all seven sites three ways: a claim with no refutation within 400 characters fails, a silently deleted correction fails, and `estimateCost.ts`'s exclusion is asserted in **both** directions. All three watched failing. The 400-character window is four times the observed worst case (95). | **A refuted mechanism is re-asserted, because nothing pins the correction.** Seven places said destination dispatch does better under access control *because* authorization and optimization happen in the same step. All seven are now corrected and **no test asserts that they stay corrected** — nothing went red while they were wrong, and nothing would go red if they came back | Medium — the sentence is intuitive, comfortable, and was independently written seven times | High. It is the project's stated purpose to refuse comfortable conclusions that the data does not support | whoever owns `validation/documentation.test.ts` | Recommendation **T23-R1** in `AGENT_STATUS.md`: a guard asserting no file carries the claim without carrying the refutation, watched failing by re-inserting the old sentence. `estimateCost.ts` is explicitly excluded, because its version of the sentence is descriptive and true | Any document or docstring reintroduces the performance claim, or the guard is written without being watched failing |

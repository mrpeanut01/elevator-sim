# Your filled-in parameters. `main.parameters.example.json` is the committed one and holds no
# secret; this is the copy that may hold two, and it must never be committed.
azure/main.parameters.json

# Compiled ARM output, if you ever run `az bicep build` with `--outfile`.
*.json.bicep
azure/main.json
